"use strict";
/*
this interface is not used in final solution..NumbersCollection,
CharactersCollection and LinkedList classes have similar looking
functionality so we used preferred inheritance..



interface Sortable {
  length : number;
  compare(leftIndex: number, rightIndex: number): boolean;
  swap(leftIndex: number, rightIndex: number): void;
}

 */ 
